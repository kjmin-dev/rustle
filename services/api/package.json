{
    "name": "api",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "prepare": "",
        "dev": "RUST_LOG=info,axum_tracing_example=debug,tower_http=debug cargo watch -x run",
        "build": "cargo build --release",
        "format": "cargo fmt"
    }
}
