{
    "name": "api",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "prepare": "",
        "dev": "cargo watch -x run",
        "build": "cargo build --release",
        "format": "cargo fmt"
    }
}
